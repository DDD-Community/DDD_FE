//import { redirect } from "react-router";

export const requireAuth = async () => {
  // TODO: 로그인 여부, 권한 체크 로직 구현
  // const { user } = useUser();
  // if(!user?.isAdmin) {
  //     throw redirect("/login");
  // }
  return
}
